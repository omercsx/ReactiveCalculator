import { StyleSheet, Text, View } from 'react-native';

const GravityCalculator = () => {
	return (
		<View>
			<Text>GravityCalculator</Text>
		</View>
	);
};

export default GravityCalculator;

const styles = StyleSheet.create({});
