import { StyleSheet, Text, View } from 'react-native';

const NormalCalculator = () => {
	return (
		<View>
			<Text>NormalCalculator</Text>
		</View>
	);
};

export default NormalCalculator;

const styles = StyleSheet.create({});
