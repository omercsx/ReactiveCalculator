import { StyleSheet, Text, View } from 'react-native';
const Home = () => {
	return (
		<View>
			<Header />
			<Text>Home</Text>
		</View>
	);
};

export default Home;

const Header = () => {
	return <Text>Header</Text>;
};

const styles = StyleSheet.create({});
